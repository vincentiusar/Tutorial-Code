// .load to load file js
// .save to save session

const nama = 'saya'
const umur = 9
const sayHalo = (nama, umur) => {
console.log(`halo, nama saya ${nama}, umur saya ${umur}`);
}
sayHalo
sayHalo(nama, umur)
const x = 9;
const y = 19;
var hitungTambah = (a, b) => {
   console.log(a + b);
}
hitungTambah(x, y);

hitungTambah(x, y);